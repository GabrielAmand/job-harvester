[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "job-harvester"
version = "0.7.0"
description = "Collect public job listings into a local SQLite database"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
dependencies = []

[project.scripts]
job-harvester = "job_harvester.cli:main"
